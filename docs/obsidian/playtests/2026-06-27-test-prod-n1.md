# 2026-06-27 - Test production n°1 - v0.1.1

Date : 2026-06-27 21:08 +04:00

## Verdict

**NO-GO pour validation production complète.**

Raison :

- build web v0.1.1 OK ;
- serveur local OK ;
- boucle combat serveur jouable de bout en bout OK ;
- Chrome/PWA et rendu canvas non validés visuellement : l'outil navigateur a timeout ;
- incohérence confirmée : `activeEvent = eveil-fournaise` alors que le départ est Ouest.

## Périmètre

- Client web PWA v0.1.1.
- Serveur Colyseus local séparé.
- Pas de déploiement Fly.io / Cloudflare.
- Test local prod-like : `http://localhost:5173` + `ws://127.0.0.1:2567`.

## Pré-vol

| Check | Résultat | Détail |
| --- | --- | --- |
| `corepack pnpm install` | OK | Lockfile à jour. |
| `corepack pnpm typecheck` | KO puis OK | KO serveur : `startZone` possiblement `undefined` dans `ReunionWorldRoom.ts`. Fix : helper `resolveStartZone()`. |
| `corepack pnpm --filter @riw/game-client typecheck` | OK | Client TS vert. |
| `corepack pnpm --filter @riw/game-client lint` | OK | ESLint client vert. |

## Build release web

Commande :

```powershell
corepack pnpm stop:web
Remove-Item -Recurse -Force .\apps\game-client\dist -ErrorAction SilentlyContinue
corepack pnpm release:web
```

Résultat :

- zip : `output/reunion-island-wisdom-web-0.1.1-20260627-205439.zip`
- poids : 24,31 Mo ;
- racine zip : `index.html`, `sw.js`, `manifest.webmanifest`, `README_INSTALL.txt` ;
- `icons/` présent : 5 fichiers ;
- `sw.js` contient `riw-app-shell-v0.1.1` ;
- GLB monolithique encore présent : `assets/terrain/lareunion/lareunion-relief-map.glb` = 17,42 Mo ;
- total GLB dans zip : 45,89 Mo.

## Cible serveur

Local prod-like :

- `corepack pnpm launch:web:no-browser` lancé ;
- client : `http://127.0.0.1:5173/` -> HTTP 200 ;
- serveur : `http://127.0.0.1:2567/health` -> HTTP 200.

Prod réelle :

- non testée ;
- aucun déploiement Fly.io / Cloudflare effectué ;
- `.env.production` pointe vers `VITE_GAME_SERVER_URL=wss://riw-game-server.fly.dev`.

Rappel :

- sans serveur Colyseus, les cibles et le combat restent vides côté client.

## Checklist playtest

### Démarrage

- [ ] Spawn à l'Ouest, bandeau "Saint-Paul / Saint-Gilles", pas sous l'eau : **NON VÉRIFIÉ VISUEL**. Vérifié serveur : spawn Ouest autour de `x=-77.5`, `z=5.5`.
- [x] Statut serveur online : **OK serveur** (`/health 200`, connexion Colyseus OK).
- [ ] Console navigateur sans erreur rouge : **NON VÉRIFIÉ**. L'outil navigateur time out avant capture.

### Déplacement / collision

- [ ] ZQSD/WASD + souris caméra : **NON VÉRIFIÉ VISUEL**.
- [ ] `Espace` = saut, retombe au sol : **NON VÉRIFIÉ VISUEL**.
- [ ] Reliefs solides, pas de traversée : **NON VÉRIFIÉ VISUEL**.
- [ ] Pas de blocage anormal sur le sentier : **NON VÉRIFIÉ VISUEL**.

### Combat

- [x] Les 3 cibles Ouest existent côté serveur : **OK** (`galet-roulant-1`, `remous-ravine-1`, `embacle-ravine-1`).
- [ ] Cibles visibles, ancrées au sol : **NON VÉRIFIÉ VISUEL**.
- [x] Aucun dégât sans attaquer : **OK** (PV 100 -> 100 après attente).
- [x] Attaque à portée : **OK** (`Galet roulant` 35 -> 17, dégâts 18).
- [ ] Flash + son d'impact : **NON VÉRIFIÉ VISUEL/AUDIO**.
- [x] Cible détruite : **OK serveur** (`targetDeadAfterLoop: true`).
- [x] Récompense souvenir : **OK serveur** (`Galet poli` reçu via `targetDefeated`).
- [x] Respawn cible : **OK** à froid après délai (`Galet roulant` vivant, 35/35).
- [x] Dégât après aggro sur coup : **OK** (PV joueur 100 -> 92 après attaque).
- [ ] Flash rouge écran + son joueur blessé : **NON VÉRIFIÉ VISUEL/AUDIO**.
- [ ] PV à 0, message "Vaincu", respawn joueur Ouest PV pleins : **NON VÉRIFIÉ**. Non atteint dans test réseau court.
- [ ] Mobile joystick + boutons E/F : **NON VÉRIFIÉ VISUEL**.

### Multijoueur

- [x] Deux clients serveur connectés : **OK**.
- [x] Chat reçu : **OK** (`test prod n1` vu par le client A).
- [ ] Autre joueur visible + nametag : **NON VÉRIFIÉ VISUEL**.
- [x] Pas de PvP : **OK par conception testée statiquement**. Aucun message d'attaque joueur-joueur, `attackIntent` ne prend qu'un `targetId` de cible PvE.

### PWA / Chrome

- [ ] Icône "Installer" Chrome : **NON VÉRIFIÉ**.
- [ ] App installée lancée : **NON VÉRIFIÉ**.
- [ ] Reload -> version installée v0.1.1 / purge SW : **NON VÉRIFIÉ CHROME**.
- [x] Fichiers nécessaires dans zip : **OK** (`manifest.webmanifest`, `sw.js`, `icons/`).
- [x] Cache SW v0.1.1 dans zip : **OK**.

### Perf

- [ ] `?perfDebug` desktop : **NON VÉRIFIÉ VISUEL**.
- [ ] `?perfDebug` mobile : **NON VÉRIFIÉ**.
- [ ] `?mapDebug` relief/cibles posées : **NON VÉRIFIÉ VISUEL**.
- [x] Dette perf confirmée : **OK**. GLB monolithique 17,42 Mo encore dans le zip.

## Sécurité

- [x] Attaque hors portée rejetée serveur : **OK** (`remous-ravine-1` reste 55/55).
- [x] Spam attaque rejeté par cooldown serveur : **OK** (3 attaques immédiates = 1 seul coup, 35 -> 17).
- [x] Aucun secret/API key côté client : **OK scan statique**. Seul `VITE_GAME_SERVER_URL` public.
- [x] Messages entrants validés Zod : **OK code** (`move`, `chat`, `interact`, `attack` via `safeParse`).
- [x] Récompense décidée serveur : **OK code + test** (`targetDefeated` envoyé par `ReunionWorldRoom` après kill serveur).

## Incohérences confirmées

- **P2** : `activeEvent = "eveil-fournaise"` côté serveur alors que la zone de départ est Ouest.
- **P2** : titre interne `START_QUEST_TITLE = "Éveil de la Fournaise"` encore présent dans `hud.ts`.
- **P3** : `?visualZone=fournaise` déplace le client côté Fournaise, mais le serveur reste sur spawn Ouest. Desync attendue.
- **P3** : souvenir notifié uniquement ; pas encore stocké en inventaire serveur.

## Bugs / risques classés

### P0

- Aucun P0 confirmé.

### P1

- Validation Chrome/PWA impossible dans ce tour : outil navigateur timeout. Bloque le GO production réel.

### P2

- `activeEvent` / titre quête Fournaise incohérents avec départ Ouest.
- GLB monolithique `lareunion-relief-map.glb` encore dans le zip : 17,42 Mo.

### P3

- `?visualZone=fournaise` connu comme mode debug désynchronisé serveur.
- Inventaire souvenir absent : notification seule.

## Captures

Aucune capture fiable.

Cause : le navigateur intégré répond à `tabs.list`, mais timeout lors de l'ouverture/capture du jeu local.

## Critère GO

État :

- aucun P0/P1 produit confirmé ;
- boucle combat serveur OK ;
- connexion serveur locale stable OK ;
- PWA installable non vérifiée ;
- rendu visuel non vérifié.

Verdict final : **NO-GO jusqu'à une passe manuelle Chrome/PWA + capture visuelle du gameplay**.

---

## MAJ Claude 2026-06-27 (correctifs post-rapport)

Deux risques P2 traités (validation navigateur Opera GX a aussi echoue chez Claude : extension incapable d'initialiser un groupe d'onglets — meme classe que le timeout). Verdict NO-GO **inchange** : la validation rendu/PWA reste a faire manuellement.

- **P2 activeEvent** : `ReunionWorldRoom.activeEvent` derive desormais de la zone reelle (`eveil-${startZone.id}`) -> coherent avec le depart Ouest (`eveil-saint-paul-saint-gilles`). Plus de "eveil-fournaise" en depart Ouest.
- **P2 GLB dans le zip** : le GLB monolithique (~17 Mo) n'est PAS charge en prod (gate `shouldUseChunkStreaming` deja en place, world.ts) ; je le retire en plus du **package** dans `tools/package-web-release.ps1` (staging -> zip). `dist/` et le dev gardent le fichier (fallback). Effet au prochain `release:web` : zip allege (~17 Mo de moins, decompresse). Les chunks RGE ALTI restent (necessaires au streaming).

Reste P2 lie (non touche, domaine HUD/objectif Codex) :

- `START_QUEST_TITLE = "Éveil de la Fournaise"` dans `hud.ts` alors que les etapes d'objectif sont Ouest (Tatie Snack -> Car Jaune -> Maido). Fix rapide possible (renommer le titre) sur demande.

A relancer sous Windows apres ces edits : `corepack pnpm typecheck` (serveur) + `corepack pnpm release:web` (verifier le nouveau poids du zip, absence de `lareunion-relief-map.glb`).

## MAJ Claude 2026-06-27 (rendu confirme + fix vegetation)

- **Rendu visuel CONFIRME** : screenshot Shan (jeu lance, depart Ouest) montre terrain RGE ALTI, sentier, props, joueur au sol, HUD barre de vie (PV 100). -> le P1 "rendu canvas non valide" est leve cote affichage. Reste a verifier a la main : installation/maj PWA dans Chrome.
- **Bug trouve + corrige** : palmiers Ouest flottants (vegetation posee sur `reliefCollision` au lieu des chunks visibles). Fix `westVegetation.ts` -> pose sur heightfields de chunks. Voir [[iterations/2026-06-27-fix-vegetation-flottante]]. A confirmer au reload.
