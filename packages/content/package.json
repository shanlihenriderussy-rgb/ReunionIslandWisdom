{
  "name": "@riw/content",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "validate:content": "tsx scripts/validate-content.ts",
    "build": "pnpm validate:content && tsc -p tsconfig.json",
    "lint": "eslint src",
    "typecheck": "pnpm validate:content && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@riw/shared": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "tsx": "^4.22.4"
  }
}
